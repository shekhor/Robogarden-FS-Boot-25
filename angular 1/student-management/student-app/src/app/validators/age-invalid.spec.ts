import { AgeInvalid } from './age-invalid';

describe('AgeInvalid', () => {
  it('should create an instance', () => {
    const directive = new AgeInvalid();
    expect(directive).toBeTruthy();
  });
});
